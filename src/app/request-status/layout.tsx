export const metadata = {
  title: "Private request status",
  robots: { index: false, follow: false },
  alternates: { canonical: "/request-status" },
  referrer: "no-referrer",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
